# Copy to .env and fill in. NEVER commit your real .env (it's git-ignored).
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional overrides
COACH_MODEL=claude-opus-4-8
# USE_ST=1            # set to use sentence-transformers embeddings for the RAG KB
